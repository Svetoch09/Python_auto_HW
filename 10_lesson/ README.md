# Lesson 10 - Allure + PageObject 🚀

Это проект, посвященный отработке навыков использования фреймворков для автоматизации тестирования с применением:
* **Allure Report:** для генерации красивых и подробных отчетов о выполнении тестов.
* **Page Object Model (POM):** для создания поддерживаемой и читаемой структуры тестов.

---

### ⚙️ Установка и запуск

1. Установка зависимостей

pip install -r requirements.txt

2. Запуск тестов с генерацией Allure-результатов
Для запуска тестового набора и одновременной генерации Allure-результатов используйте команду pytest, указав директорию для сохранения результатов:

Bash
pytest 10_lesson --alluredir=allure-results

3. Просмотр отчёта Allure
После завершения тестов вы можете запустить локальный сервер для просмотра отчета в браузере:

Bash
allure serve allure-results

🗑️ Игнорируемые файлы (.gitignore)
Папки allure-results и allure-report не коммитим.

Убедитесь, что ваш файл .gitignore содержит:

allure-results/

allure-report/